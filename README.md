# Hand Gesture Streamer

Flutter приложение для захвата жестов руки с телефона и отображения видео на ноутбуке в реальном времени.

## Возможности

- 📱 Захват видео с камеры телефона
- 🖥️ Отображение видео на ноутбуке в реальном времени
- 🔌 WebSocket соединение для передачи данных
- 📦 Готово к публикации на любых платформах

## Структура проекта

- `mobile_app/` - Мобильное приложение для Android/iOS
- `desktop_app/` - Десктопное приложение для Windows/macOS/Linux

## Требования

- Flutter SDK 3.0.0 или выше
- Android Studio / Xcode (для мобильной разработки)
- Visual Studio / Xcode / Linux dev tools (для десктопной разработки)

## Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
# Установите зависимости для мобильного приложения
cd mobile_app
flutter pub get

# Установите зависимости для десктопного приложения
cd ../desktop_app
flutter pub get
```

### 2. Запуск десктопного приложения

```bash
cd desktop_app

# Windows
flutter run -d windows

# macOS
flutter run -d macos

# Linux
flutter run -d linux
```

После запуска приложение покажет IP адрес и порт сервера. Запишите этот IP адрес.

### 3. Запуск мобильного приложения

```bash
cd mobile_app

# Android
flutter run

# iOS
flutter run -d ios
```

### 4. Подключение

1. В мобильном приложении введите IP адрес ноутбука (который показан в десктопном приложении)
2. Нажмите "Подключиться"
3. После успешного подключения нажмите "Начать трансляцию"
4. Разрешите доступ к камере при первом запуске

## Публикация

### Android (APK)

```bash
cd mobile_app
flutter build apk --release
```

APK файл будет находиться в `mobile_app/build/app/outputs/flutter-apk/app-release.apk`

### Android (App Bundle для Google Play)

```bash
cd mobile_app
flutter build appbundle --release
```

AAB файл будет находиться в `mobile_app/build/app/outputs/bundle/release/app-release.aab`

### iOS

```bash
cd mobile_app
flutter build ios --release
```

Затем откройте Xcode и создайте архив для публикации в App Store.

### Windows

```bash
cd desktop_app
flutter build windows --release
```

Собранное приложение будет в `desktop_app/build/windows/x64/runner/Release/`

### macOS

```bash
cd desktop_app
flutter build macos --release
```

### Linux

```bash
cd desktop_app
flutter build linux --release
```

## Сетевая настройка

⚠️ **Важно**: Оба устройства (телефон и ноутбук) должны быть подключены к одной Wi-Fi сети.

Если подключение не работает:
1. Проверьте, что оба устройства в одной сети
2. Убедитесь, что файрвол на ноутбуке разрешает входящие соединения на порту 8080
3. Проверьте IP адрес ноутбука в настройках сети

## Технические детали

- **Протокол**: WebSocket
- **Формат видео**: JPEG кадры (сжатие 70%)
- **Разрешение**: Medium (зависит от устройства)
- **Порт по умолчанию**: 8080

## Лицензия

Этот проект создан для публикации на любых платформах.
